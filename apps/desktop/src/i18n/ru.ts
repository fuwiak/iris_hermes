import { defineLocale } from './define-locale'

// Russian is supported for agent/gateway catalogs (`display.language: ru`).
// Desktop UI strings fall back to English until a full translation ships.
export const ru = defineLocale({})
